import ActiveJobClient from './ActiveJobClient';

export const dynamic = 'force-dynamic';
export const fetchCache = 'force-no-store';

export default function ActiveJobPage() {
    return <ActiveJobClient />;
}
