# Optimize for Cloudflare Pages build
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Disable audit to speed up install
audit=false

# Disable fund messages
fund=false

# Enable workspaces (required for monorepo)
workspaces=true
