# Optimize for Cloudflare Pages build
# Skip optional dependencies to reduce install time and avoid conflicts
optional=false

# Don't run scripts during install (security + speed)
ignore-scripts=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Disable audit to speed up install
audit=false

# Disable fund messages
fund=false
